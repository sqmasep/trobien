@import "../palette/main"

.btn
    display block
    width fit-content
    cursor pointer
    padding-inline 1em
    padding-block .5em
    font-weight lighter
    text-decoration none
    border none
    &:focus-visible
        outline-offset .25em
        outline-width @outline-offset * 2
        outline-color primary
